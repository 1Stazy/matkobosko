<UserControl x:Class="KinoApp.Controls.NeonLogo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="400">
    
    <Grid Background="Transparent">
        <Viewbox Stretch="Uniform">
            <Border BorderBrush="#FF00E5FF" BorderThickness="3" CornerRadius="10" Padding="20,10" Margin="10">
                <Border.Effect>
                    <DropShadowEffect Color="#FF00E5FF" BlurRadius="20" ShadowDepth="0" Opacity="0.8"/>
                </Border.Effect>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="NEON" 
                               FontFamily="Segoe UI Light" 
                               FontSize="60" 
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="0,0,10,0">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#FF00E5FF" BlurRadius="30" ShadowDepth="0" Opacity="1"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <TextBlock Text="CINEMA" 
                               FontFamily="Segoe UI Light" 
                               FontSize="60" 
                               FontWeight="Normal"
                               Foreground="White">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#FFFF00D4" BlurRadius="30" ShadowDepth="0" Opacity="1"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Viewbox>
    </Grid>
</UserControl>